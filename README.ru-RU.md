# Folkers
[English](README.md) | Русский

Folkers — веб-приложение для удобного управления приватной информацией о людях.  
Оно предоставляет быстрый и интуитивный API, современный интерфейс и безопасный доступ.

Основное взаимодействие основано на трёх ролях: _watcher_, _editor_, _admin_.  
Каждая роль имеет свои разрешения и особенности:
- **Watcher (Наблюдатель).** Базовый пользователь без прав на редактирование, может только просматривать записи на главной странице.
- **Editor (Редактор).** Может создавать новые записи и редактировать (только свои).
- **Admin (Администратор).** Может просматривать, создавать и редактировать любые записи (в том числе не свои), а также создавать, редактировать и удалять пользователей (только принадлежащих ему).

Кроме этого, в системе Folkers есть скрытая роль: _static admin_.
Static Admin — это статический администратор, который имеет права роли _admin_, может выполнять любые действия и не может быть удалён или изменён.  
Static Admin настраивается через переменные окружения (см. ниже).

## Возможности
- **🚀 Быстрый.** Всё работает быстро без дополнительных настроек.
- **🔥 Удобный.** Простой интерфейс с понятными инструкциями.
- **🧩 Современный.** Построен на совремённом стеке: Rust (backend) и SvelteKit (frontend).
- **✔️ Безопасный.** Данные защищены авторизацией через JWT.

## Стек разработки
> **Backend:**
> - Язык: `Rust`
> - Веб-фреймворк: `Axum`
> - Авторизация: `Json Web Token`
> - База данных: `SurrealDB`
> - Алгоритм хеширования: `argon2`
>
> **Frontend:**
> - Production сервер: `Nginx`
> - Фреймворк: `SvelteKit`
> - Язык: `TypeScript`
> - Runtime: `Bun`
> - Компоненты: `Skeleton UI`
> - Markdown-компилятор: `Mdsvex`

## Установка
1. Клонируйте репозиторий:
```command
git clone https://github.com/mealet/folkers
cd folkers
```
2. Установите [Docker](https://www.docker.com/) с оффициального сайта.
3. Установите переменные окружения:
```env
FOLKERS_JWT_SECRET = # секретная строка для jwt
FOLKERS_BASE64_SALT = # base64-представление соли для хеша
FOLKERS_UPLOADS_DIR = # путь к директории с загруженными медиа (опционально)

FOLKERS_DB_USERNAME = # имя пользователя БД (по умолчанию: root) (ОЧЕНЬ РЕКОМЕНДУЕТСЯ ИЗМЕНИТЬ)
FOLKERS_DB_PASSWORD = # пароль БД (по умолчанию: root) (ОЧЕНЬ РЕКОМЕНДУЕТСЯ ИЗМЕНИТЬ)
FOLKERS_DB_NAMESPACE = # пространство БД (surrealdb) (по умолчанию: folkers)
FOLKERS_DB_DATABASE = # имя базы БД (surrealdb) (по умолчанию: folkers)
FOLKERS_DB_ENDPOINT = # адрес БД (по умолчанию: localhost:8080, НЕ МЕНЯТЬ БЕЗ НЕОБХОДИМОСТИ)

FOLKERS_STATIC_ADMIN_USERNAME = # имя статического администратора (по умолчанию: mealet)
FOLKERS_STATIC_ADMIN_PASSWORD = # пароль статического администратора (по умолчанию: mealet)
```
4. Соберите образы:
```command
docker compose build
```
5. Запустите приложение:
```command
docker compose up -d # уберите -d, если хотите работать в интерактивном режиме
```
6. Откройте http://localhost в браузере.

## License
Проект распространяется под лицензией Apache 2.0. <br />
Подробнее см. файл [LICENSE](LICENSE)
